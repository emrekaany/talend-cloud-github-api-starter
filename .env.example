# Copy values into your shell or local secret manager. This project does not
# load .env files automatically and never accepts the PAT as a CLI argument.
TALEND_BASE_URL=https://api.eu.cloud.talend.com
TALEND_TOKEN=
